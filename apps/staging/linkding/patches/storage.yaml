apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: storage-staging
  resources:
    requests:
      storage: 1Gi
