apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: adguardhome
spec:
  interval: 30m
  chart:
    spec:
      chart: adguardhome
      version: "0.1.13"
      sourceRef:
        kind: HelmRepository
        name: adguardhome
  values:
